overlay: 1.0.0
info:
  title: Overlay beta.yaml => beta_after.yaml
  version: 0.0.0
actions:
  - target: $["info"]["description"]
    update: Supabase API
  - target: $["info"]["title"]
    update: Supabase API
  - target: $["info"]["contact"]
    remove: true
